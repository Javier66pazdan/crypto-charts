import { CryptoProcentagePipe } from './crypto-percent.pipe';

describe('CryptoProcentagePipe', () => {
  it('create an instance', () => {
    const pipe = new CryptoProcentagePipe();
    expect(pipe).toBeTruthy();
  });
});
